# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Docker integration
docker>=7.0.0

# Database
aiosqlite>=0.19.0

# YAML parsing (for compose + config files)
PyYAML>=6.0

# System resource detection
psutil>=5.9.0

# Scheduled campaigns
APScheduler>=3.10.0

# Server-Sent Events for log streaming
sse-starlette>=1.6.0

# WebSocket support (included with uvicorn[standard])
websockets>=12.0

# Prometheus metrics exposition
prometheus-client>=0.20.0

# HTTP client for webhook alerts and testing
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Code quality
black>=24.0.0
ruff>=0.4.0
